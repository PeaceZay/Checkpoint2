const express = require("express");
let router = new express.router();
let userController = require("../controllers/clientCTRL");

// get summary of clients

// get details of a single client given the id

// update a client, given its id

// delete a client